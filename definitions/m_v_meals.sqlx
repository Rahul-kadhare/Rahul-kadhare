config {
    type: "view",
    schema : "row_dataset",
    materialized: true
}
select
    distinct meal_id,
    meal_name,
    meal_type,
    calories
from 
    ${ref("meals")}
where 
    calories >= 500